services:
  kiro-node:
    build: .
    container_name: kiro-node
    ports:
      - "8990:8990"
    environment:
      API_KEY: ${API_KEY:-sk-kiro-node}
      ADMIN_API_KEY: ${ADMIN_API_KEY:-sk-admin}
    volumes:
      - ./config/:/app/config/
    restart: unless-stopped
